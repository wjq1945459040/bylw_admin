# 端口
server:
  port: 8202
spring:
  # json转换
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  #nacos配置
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  # redis基本配置
  redis:
    port: 6813
    host: 121.41.164.125
    database: 0
    timeout: 1800000
    lettuce:
      pool:
        max-active: 20
        max-wait: -1
        max-idle: 5
        min-idle: 0

# 阿里云短信配置，自定义
